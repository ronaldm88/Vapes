<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "BeerMe", root_path, class: "navbar-brand" %>
    </div>

    <center>
      <div class="navbar-collapse collapse" id="navbar-main">
        <ul class="nav navbar-nav">
          <li><%= link_to "Beer", beers_path %></li>
          <li><%= link_to "About", beers_path %></li>
          <li><%= link_to "Contact", beers_path %></li>
        </ul>
        <% if user_signed_in? %>
          <div class="navbar-form navbar-right white-text">
            Logged in as <strong><%= current_user.email %></strong>
            <%= link_to "Logout", destroy_user_session_path, class:"btn btn-warning" %>
          </div>

        <% else %>
          <%= form_for @user, :url => new_user_session_path, :html => {class: "navbar-form navbar-right"} do |f|%>
            <div class="form-group">
              <%= f.text_field :email, placeholder:"Email", class:"form-control" %>
            </div>

            <div class="form-group">
              <%= f.password_field :password, placeholder:"Password", class:"form-control" %>
            </div>

            <%= f.submit "Login", class:"btn btn-success" %>

            <%= link_to "Signup", new_user_registration_path, class:"btn btn-primary" %>
          <% end %>
        <% end %>
      </div><!--/.navbar-collapse -->
    </center>
  </div>
</nav>
