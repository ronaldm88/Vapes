<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "BeerMe", root_path, class: "navbar-brand" %>
    </div>

    <center>
      <div class="navbar-collapse collapse" id="navbar-main">
        <ul class="nav navbar-nav">
          <li><%= link_to "Beer", beers_path %></li>
          <li><%= link_to "CheckIns", check_ins_path %></li>
          <%= users_link_to if user_signed_in? %>
          <li><%= link_to "About", about_path %></li>
          <li><%= link_to "Contact", contact_path %></li>
        </ul>

        <% if user_signed_in? %>
          <div class="navbar-form navbar-right white-text">
            Logged in as <strong><%= current_user.email %></strong>
            <%= link_to "Profile", user_path(current_user), class:"btn btn-info" %>
            <%= link_to "Logout", destroy_user_session_path, class:"btn btn-warning", method: "delete" %>
          </div>

        <% else %>
          <div class="navbar-form navbar-rightf">
            <%= link_to "Login", new_user_session_path, class:"btn btn-success" %>

            <%= link_to "Signup", new_user_registration_path, class:"btn btn-primary" %>
          </div>
        <% end %>
      </div><!--/.navbar-collapse -->
    </center>
  </div>
</nav>
